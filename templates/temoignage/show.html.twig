{% extends 'admin/layout.html.twig' %}
{% block title %} {{parent()}} | Temoignage {% endblock %}

{% block dataStructured %}{% endblock %}
{% block body %}
    <h1>Temoignage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ temoignage.id }}</td>
            </tr>
            <tr>
                <th>Creation</th>
                <td>{{ temoignage.creation ? temoignage.creation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ temoignage.name }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ temoignage.location }}</td>
            </tr>
            <tr>
                <th>Video</th>
                <td>{{ temoignage.video }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ temoignage.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('temoignage_index') }}">back to list</a>

    <a href="{{ path('temoignage_edit', {'id': temoignage.id}) }}">edit</a>

    {{ include('temoignage/_delete_form.html.twig') }}
{% endblock %}

