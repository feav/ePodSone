{{ form_start(form) }}
    <div class="my-custom-class-for-errors">
        {{ form_errors(form) }}
    </div>

    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Modifier') }}</button>
{{ form_end(form) }}
