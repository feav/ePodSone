{% extends 'base.html.twig' %}

{% block title %}Abonnement{% endblock %}

{% block body %}
    <h1>Abonnement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ abonnement.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ abonnement.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ abonnement.description }}</td>
            </tr>
            <tr>
                <th>Echeance</th>
                <td>{{ abonnement.duree }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ abonnement.price }}</td>
            </tr>
            <tr>
                <th>Trial_day</th>
                <td>{{ abonnement.trialDay }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('abonnement_index') }}">back to list</a>

    <a href="{{ path('abonnement_edit', {'id': abonnement.id}) }}">edit</a>

    {{ include('abonnement/_delete_form.html.twig') }}
{% endblock %}
